# The core pipeline (generator.py, detector.py, baseline.py, diagnoser.py,
# cost_estimator.py, recovery_estimator.py, incident_memory.py, prioritizer.py,
# build_dashboard.py, live_dashboard.py, control_server.py) uses only the
# Python standard library. This file is not required to run detection,
# diagnosis, or the dashboard.
#
# Requires Python 3.11+ (uses datetime.UTC and `X | Y` syntax).
#
# openai is optional and only required for the two AI copywriting features
# (never for detection or diagnosis):
#   - explainer.py --use-openai        (AI-written explanation)
#   - chat_service.py / control_server.py  (dashboard conversational assistant)
openai>=1.66.0
